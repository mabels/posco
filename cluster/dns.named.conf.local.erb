<% zones.each do | zone| %>
<%= zone.keycontent %>
zone "<%= zone.name %>" {
	type master;
	file "/var/lib/bind/<%= zone.name %>.zone";
	allow-query { any; };
        allow-update { key "<%="#{zone.name}-key" %>";};
};
<% end %>
